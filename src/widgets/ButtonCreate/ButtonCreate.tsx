import styles from './ButtonCreate.module.scss';

const ButtonCreate = () => {
    return (
        <button
            className={styles.button}
        >
            Создать
        </button>
    );
};

export default ButtonCreate;