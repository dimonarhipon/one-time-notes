import styles from './NoteCopyedModal.module.scss';

const NoteCopyedModal = () => {
    return (
        <div className={styles.text}>Ссылка скопирована</div>
    );
};

export default NoteCopyedModal;