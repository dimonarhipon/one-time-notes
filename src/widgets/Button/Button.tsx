import styles from './Button.module.scss';

const Button = () => {
    return (
        <button
            className={styles.button}
        >
            Создать
        </button>
    );
};

export default Button;